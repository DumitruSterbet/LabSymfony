<form method="post" action="{{ path('tara_prod_delete', {'id': tara_prod.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tara_prod.id) }}">
    <button class="btn"><h3 style="color: #6b5b95;">Delete</h3></button>
</form>
